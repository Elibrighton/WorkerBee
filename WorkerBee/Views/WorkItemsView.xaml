<Window x:Class="WorkerBee.Views.WorkItemsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkerBee.Views"
        mc:Ignorable="d"
        Title="Work Items" Height="450" Width="800" 
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" MaxHeight=" 25">
            <Label Content="Add work item" />
            <TextBox Text="{Binding AddWorkItemTextBoxText}" MinWidth="200" Padding="10,0,10,0" />
            <Button Content="Save" Command="{Binding SaveButtonCommand}" MinWidth="25" />
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Label Content="Work items" />
            <ListBox ItemsSource="{Binding WorkItemsListBoxItemSource}"
                  DisplayMemberPath="Description" 
                  SelectedItem="{Binding SelectedWorkItemsListBoxItem}" />
            <Button Content="Delete" Command="{Binding DeleteButtonCommand}" MinWidth="25" />
        </StackPanel>
    </Grid>
</Window>
